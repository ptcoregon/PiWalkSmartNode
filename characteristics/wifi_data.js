
var currentSSID = "currentSSID!";

var newSSID = "newSSID!";

var password = "password!";

var status = 0;


module.exports = {
	
	currentSSID : currentSSID,
	
	newSSID : newSSID,
	
	password : password,
	
	status : 0x05,
	
}
